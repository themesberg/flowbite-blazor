@inherits Flowbite.Base.FlowbiteComponentBase

@if (ModelContext.IsOpen)
{
    <div class="@CombineClasses(BaseClasses)" role="listbox" style="@Style" @attributes="AdditionalAttributes">
        @ChildContent
    </div>
}
