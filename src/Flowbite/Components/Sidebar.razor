@using Flowbite.Base
@inherits FlowbiteComponentBase

<nav aria-label="Sidebar" class="@GetClasses()" hidden="@(IsCollapsed && CollapseBehavior == SidebarCollapseBehavior.Hide)">
    <div class="h-full px-3 py-4 overflow-y-auto bg-gray-50 dark:bg-gray-800 rounded-lg">
        @ChildContent
    </div>
</nav>
