@using Flowbite.Base
@using Flowbite.Common
@inherits FlowbiteInputBase<TValue>
@typeparam TValue

<div class="@GetWrapperClasses()" style="@Style">
    @if (!string.IsNullOrEmpty(AddonLeft))
    {
        <span class="@GetAddonClasses()">@AddonLeft</span>
    }
    <div class="@GetFieldClasses()">
        @if (Icon != null)
        {
            <div class="@GetIconClasses()">
                <DynamicComponent Type="@Icon.GetType()" Parameters="@(new Dictionary<string, object>
                {
                    { "class", "h-5 w-5 text-gray-500 dark:text-gray-400" },
                    { "aria-hidden", true }
                })" />
            </div>
        }
        @if (RightIcon != null)
        {
            <div class="@GetRightIconClasses()" data-testid="right-icon">
                <DynamicComponent Type="@RightIcon.GetType()" Parameters="@(new Dictionary<string, object>
                {
                    { "class", "h-5 w-5 text-gray-500 dark:text-gray-400" },
                    { "aria-hidden", true }
                })" />
            </div>
        }
        @if (Behavior == InputBehavior.OnInput)
        {
            <input type="@Type"
                   class="@GetInputClasses()"
                   value="@GetDisplayValue()"
                   @oninput="HandleInputAsync"
                   placeholder="@Placeholder"
                   disabled="@Disabled"
                   required="@Required"
                   pattern="@Pattern"
                   title="@Title"
                   inputmode="@InputMode"
                   data-testid="flowbite-text-input"
                   @attributes="AdditionalAttributes" />
        }
        else
        {
            <input type="@Type"
                   class="@GetInputClasses()"
                   value="@GetDisplayValue()"
                   @onchange="HandleChangeAsync"
                   placeholder="@Placeholder"
                   disabled="@Disabled"
                   required="@Required"
                   pattern="@Pattern"
                   title="@Title"
                   inputmode="@InputMode"
                   data-testid="flowbite-text-input"
                   @attributes="AdditionalAttributes" />
        }
    </div>
    @if (!string.IsNullOrEmpty(AddonRight))
    {
        <span class="@GetAddonClasses(false)">@AddonRight</span>
    }
</div>
@if (!string.IsNullOrEmpty(HelperText))
{
    <p class="@GetHelperTextClasses()">
        @HelperText
    </p>
}
