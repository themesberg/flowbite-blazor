@using Flowbite.Base
@inherits FlowbiteComponentBase

<label class="@GetClasses()" data-testid="flowbite-label" style="@Style" @attributes="AdditionalAttributes">
    @if (!string.IsNullOrEmpty(Value))
    {
        @Value
    }
    else
    {
        @ChildContent
    }
</label>
